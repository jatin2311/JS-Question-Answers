// 13. Write a script that prompt the user to enter base and height of the triangle and calculate an area of a
// triangle (area = 0.5 x b x h).

let b = prompt();
let h = prompt();
let sqre = 0.5 * b * h;
alert(sqre);
